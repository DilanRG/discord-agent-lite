# Private character cards

Character-card files are runtime configuration and are intentionally excluded from this public repository.

Place your local JSON card in this directory, keep it untracked, and set `CHARACTER_FILE` in `.env`. The loader accepts a plain JSON object or Character Card V2 data. Keep the file writable only by the service administrator.
